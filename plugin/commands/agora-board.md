---
description: Show the current Agora board — agents, tasks, locks, open handoffs, recent activity.
allowed-tools: mcp__agora__agora_board
---
Call `agora_board` and give me a tight summary: active vs away agents, tasks grouped by status (with owners), any active file locks, and open handoffs. Flag anything that looks stuck or unclaimed.
