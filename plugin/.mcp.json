{
  "mcpServers": {
    "agora": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/agora_mcp.py", "--workspace", "${AGORA_WORKSPACE:-${HOME}/.agora/main}", "--name", "Agora"],
      "env": {}
    }
  }
}
