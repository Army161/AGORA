# >=1.9.0: earliest version with the mcp.server.auth OAuth 2.1 module
# (Dynamic Client Registration + PKCE) used by --oauth / start-web-connector.
mcp>=1.9.0
pydantic>=2.0

# HTTP mode (--http) imports these directly for the bearer-token middleware.
# They currently arrive transitively via mcp, but a direct import deserves a
# direct declaration — otherwise an upstream dependency change breaks --http.
starlette>=0.37
uvicorn>=0.30
