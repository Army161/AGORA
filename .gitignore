__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.DS_Store
*.agora/
.agora/

# Local session notes / scratch — never publish these
.remember/
*.local.json
.env
.env.*

# TLS material. `tailscale cert` writes the cert AND private key into the
# working directory, which is often the repo root. Never let that be committed.
*.crt
*.key
*.pem
